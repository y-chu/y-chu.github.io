# Local preview overrides only. Use with:
#   bundle exec jekyll serve --livereload --config _config.yml,_config.dev.yml
# Points absolute nav/links at the local server so tabs work during preview.
url: "http://localhost:4000"
baseurl: ""
